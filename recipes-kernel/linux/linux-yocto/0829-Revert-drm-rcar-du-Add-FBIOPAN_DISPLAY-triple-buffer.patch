From 1416dc47f0916a873b08a92ad26960a416458a58 Mon Sep 17 00:00:00 2001
From: Ryo Kataoka <ryo.kataoka.wt@renesas.com>
Date: Wed, 31 Jul 2013 14:41:24 +0900
Subject: [PATCH 0829/1083] Revert "drm: rcar-du: Add FBIOPAN_DISPLAY triple
 buffer support"

This reverts commit 535c531d88b9fb71ab3d0379c9f4caa6e6222b0f.
---
 drivers/gpu/drm/rcar-du/rcar_du_kms.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/gpu/drm/rcar-du/rcar_du_kms.c b/drivers/gpu/drm/rcar-du/rcar_du_kms.c
index 3e55c66..f54ac20 100644
--- a/drivers/gpu/drm/rcar-du/rcar_du_kms.c
+++ b/drivers/gpu/drm/rcar-du/rcar_du_kms.c
@@ -26,7 +26,7 @@
 #include "rcar_du_lvdsenc.h"
 #include "rcar_du_regs.h"
 
-static bool rcar_du_fbdev_pan = true;
+static bool rcar_du_fbdev_pan = false;
 module_param_named(fb_cma_pan, rcar_du_fbdev_pan, bool, 0444);
 
 /* -----------------------------------------------------------------------------
@@ -282,7 +282,7 @@ int rcar_du_modeset_init(struct rcar_du_device *rcdu)
 
 	fbdev = drm_fbdev_cma_init(dev, 32, dev->mode_config.num_crtc,
 				   dev->mode_config.num_connector,
-				   rcar_du_fbdev_pan ? 3 : 1);
+				   rcar_du_fbdev_pan ? 2 : 1);
 	if (IS_ERR(fbdev))
 		return PTR_ERR(fbdev);
 
-- 
1.8.3.2

